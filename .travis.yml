language: python
python: 
- "3.6"

before_install:
  - sudo add-apt-repository -y ppa:ubuntugis/ubuntugis-unstable
  - sudo apt-get install -y gdal-devel
  - sudo apt-get install -y gdal-bin

install:
  - pip install .

script:
  - pytest
